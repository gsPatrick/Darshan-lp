/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com'], // permite usar imagens do Unsplash
  },
  reactStrictMode: true, // opcional, mas recomendado
};

export default nextConfig;
